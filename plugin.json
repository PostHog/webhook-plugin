{
    "name": "Webhook",
    "url": "https://github.com/PostHog/webhook-plugin",
    "description": "Send a webhook",
    "main": "index.ts",
    "config": [
        {
            "key": "url",
            "name": "What is the webhook destination URL?",
            "type": "string",
            "default": "",
            "required": true
        }
    ]
}
